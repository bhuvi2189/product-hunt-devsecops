apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: product-hunt-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: product-hunt.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: product-hunt-service
                port:
                  number: 80
